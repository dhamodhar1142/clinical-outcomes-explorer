# Extension

Manifest V3 Chrome extension that reads the current job page, extracts job data, sends it to the backend, and shows the latest workflow result in a side panel.

## Load

1. Open `chrome://extensions`.
2. Turn on Developer Mode.
3. Choose `Load unpacked`.
4. Select `job-agent/extension`.

Set the backend URL in `background.js` if needed.
