# Worker

Playwright worker that executes a structured apply plan and stops at review by default.

## Run

```powershell
python -m worker.playwright_runner.main
```

Install Playwright separately when you want live automation:

```powershell
pip install playwright
playwright install
```
